<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrar Account Management</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-red: #DC2626;
            --dark-red: #B91C1C;
            --light-red: #FEF2F2;
            --accent-red: #F87171;
            --white: #FFFFFF;
            --gray-50: #F9FAFB;
            --gray-100: #F3F4F6;
            --gray-200: #E5E7EB;
            --gray-300: #D1D5DB;
            --gray-400: #9CA3AF;
            --gray-500: #6B7280;
            --gray-600: #4B5563;
            --gray-700: #374151;
            --gray-800: #1F2937;
            --gray-900: #111827;
            --shadow-md: 0 4px 6px -1px rgba(0,0,0,0.1), 0 2px 4px -1px rgba(0,0,0,0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--gray-50);
            color: var(--gray-900);
            margin: 0;
            min-height: 100vh;
        }
        .main-content {
            max-width: 700px;
            margin: 48px auto 0 auto;
            background: var(--white);
            border-radius: 16px;
            box-shadow: var(--shadow-lg);
            padding: 40px 32px 32px 32px;
        }
        .settings-title {
            font-weight: 700;
            font-size: 2rem;
            color: var(--primary-red);
            margin-bottom: 24px;
        }
        .btn-back {
            background: var(--gray-100);
            color: var(--gray-700);
            border: none;
            border-radius: 8px;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            margin-bottom: 24px;
            transition: background 0.2s;
        }
        .btn-back:hover { background: var(--gray-200); }
        .form-section, .table-section {
            background: var(--white);
            border-radius: 12px;
            box-shadow: var(--shadow-md);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        .btn-primary {
            background: var(--primary-red);
            color: var(--white);
            border: none;
            border-radius: 8px;
            padding: 10px 24px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.2s;
        }
        .btn-primary:hover { background: var(--dark-red); }
        .btn-danger {
            background: var(--dark-red);
            color: var(--white);
            border: none;
            border-radius: 8px;
            padding: 8px 18px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            margin-left: 8px;
        }
        .table { width: 100%; border-collapse: collapse; font-size: 15px; }
        .table th {
            background: var(--gray-50);
            padding: 16px 24px;
            text-align: left;
            font-weight: 600;
            color: var(--gray-700);
            font-size: 13px;
            text-transform: uppercase;
            border-bottom: 2px solid var(--gray-200);
        }
        .table td {
            padding: 16px 24px;
            border-bottom: 1px solid var(--gray-100);
            color: var(--gray-900);
        }
        .table tbody tr:last-child td { border-bottom: none; }
        @media (max-width: 800px) {
            .main-content { padding: 16px 4px; }
            .form-section, .table-section { padding: 1rem; }
        }
    </style>
</head>
<body>
    <div class="main-content">
        <button class="btn-back" onclick="window.location.href='/ictcoorLanding'">
            <i class="fas fa-arrow-left"></i> Back to Dashboard
        </button>
        <h1 class="settings-title">Registrar Account Management</h1>
        <div class="form-section mb-4">
            <h4 style="color:var(--gray-800);margin-bottom:18px;">Create Registrar Account</h4>
            <form id="registrarAccountForm" method="POST" action="/create-registrar-account">
                <div style="margin-bottom:18px;">
                    <label class="form-label">Full Name</label>
                    <input type="text" class="form-control" name="fullname" required style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--gray-300);margin-top:4px;">
                </div>
                <div style="margin-bottom:18px;">
                    <label class="form-label">Username</label>
                    <input type="text" class="form-control" name="username" required style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--gray-300);margin-top:4px;">
                </div>
                <div style="margin-bottom:18px;">
                    <label class="form-label">Password</label>
                    <input type="password" class="form-control" name="password" required style="width:100%;padding:10px;border-radius:8px;border:1px solid var(--gray-300);margin-top:4px;">
                </div>
                <button type="submit" class="btn-primary">Create Account</button>
            </form>
        </div>
        <div class="table-section">
            <h4 style="color:var(--gray-800);margin-bottom:18px;">Registrar Accounts</h4>
            <table class="table">
                <thead>
                    <tr>
                        <th>Full Name</th>
                        <th>Username</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (registrarAccounts && registrarAccounts.length > 0) { %>
                        <% registrarAccounts.forEach(function(account) { %>
                            <tr>
                                <td><%= account.fullname %></td>
                                <td><%= account.username %></td>
                                <td>
                                    <form method="POST" action="/delete-registrar-account" style="display:inline;">
                                        <input type="hidden" name="id" value="<%= account.id %>">
                                        <button type="submit" class="btn-danger" onclick="return confirm('Are you sure you want to delete this account?');">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr><td colspan="3">No registrar accounts found.</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>
    <script src="/js/toast.js"></script>
</body>
</html>
